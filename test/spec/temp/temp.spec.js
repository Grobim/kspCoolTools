(function() {
  'use strict';

  describe('Jasmin Working', function () {

    it('should make some dummy tests', function () {
      expect(1 === 1).toBeTruthy();
      expect(true).toBeTruthy();
      expect(1 + 1).toBe(2);
    });
  });}

)();
